# Binaries
build/bin
# Láminas intermedias del video del hero (ver scripts/landing-panels.mjs):
# se regeneran con un comando y acaban dentro del mp4.
build/landing-paneles/
*.exe
*.dll
*.so
*.dylib
sheepmaster
server

# Broken/orphaned git submodule references
deploy_temp

# Node.js
node_modules
npm-debug.log
yarn-error.log
.pnpm-debug.log

# Frontend build
frontend/dist
frontend/.next
frontend/out

# IDEs and OS junk
.vscode
.idea
.DS_Store
Thumbs.db

# Temp files
deploy_temp/
scratch/
# El contenido de seeder/ se ignora, salvo el seeder del hato demo: es la
# herramienta con la que se vuelven a capturar las imágenes de la landing
# (ver scripts/capture-landing.mjs), así que tiene que viajar con el repo.
# Va con "seeder/*" y no "seeder/": git no reincluye nada dentro de un
# directorio ya excluido.
seeder/*
!seeder/demo/
*.zip
*.log
.env
test_animals.xlsx
